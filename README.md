# Basic-GPT
BasicLLM is a from-scratch decoder-only Transformer (mini-GPT) trained on Persian literature text to generate literary-style Persian sentences and phrases. Built in PyTorch with a simple training + sampling pipeline.
